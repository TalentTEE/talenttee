# 현정 — Frontend

> **역할**: 전체 UI 구현, 더미 데이터로 선행 개발 → 백엔드 연결
> **마감**: 2026-04-17
> **의존**: 성훈 Day 1 (Auth API 스펙), 백엔드 API는 Day 3부터 점진적 연결
> **피의존**: 없음 (더미 데이터로 독립 진행)

---

## 1. 담당 범위

| # | 페이지/기능 | PRD 참조 | 우선순위 |
|---|-----------|---------|---------|
| 1 | NEAR 지갑 로그인 | FR-001 | Day 1 |
| 2 | 대시보드 (구직자/채용담당자) | 3.1 User Journey | Day 1 |
| 3 | 데이터소스 연결 UI | FR-002 | Day 1-2 |
| 4 | 이력서 생성 + 상태 표시 + 시장가치 | FR-003, FR-004 | Day 2 |
| 5 | 대화형 공고 작성 + 폼 | FR-005 | Day 2 |
| 6 | 협상 바운더리 설정 | FR-006 | Day 2 |
| 7 | 매칭 결과 + 동의 UI | FR-007, FR-008 | Day 2-3 |
| 8 | 협상 실시간 모니터링 + 사용자 개입 | FR-009 | Day 3 |
| 9 | 합의 확인 + 승인 | FR-010 | Day 3 |
| 10 | 에스크로 예치 + 잔액 | FR-011 | Day 2 |
| 11 | 협상 히스토리 열람 (ECDH 복호화) | FR-012 | Day 4 |
| 12 | 온체인 합의 조회 | Section 10 API | Day 4 |

## 2. 기술 스택

| 기술 | 용도 |
|------|------|
| Next.js + TypeScript | 웹 프레임워크 |
| UI 라이브러리 | 팀 논의 후 결정 (shadcn/ui + Tailwind 추천) |
| near-connect | NEAR 지갑 연결 |
| near-connect-hooks | React 지갑 훅 |
| tweetnacl | ECDH 복호화 (클라이언트 사이드) |

## 3. 더미 데이터 전략

**핵심 원칙**: 백엔드 API 스펙에 맞춰 더미 데이터를 준비하고, API가 준비되면 fetch로 교체만 하면 되도록 구조화.

```typescript
// lib/api.ts — 모든 API 호출을 한 곳에 집중

const USE_DUMMY = process.env.NEXT_PUBLIC_USE_DUMMY === 'true';

export async function getResume(id: string): Promise<ResumeProfile> {
  if (USE_DUMMY) return DUMMY_RESUME;
  return fetch(`/api/resume/${id}`).then(r => r.json());
}

export async function getMatchResults(seekerId: string): Promise<MatchResult[]> {
  if (USE_DUMMY) return DUMMY_MATCHES;
  return fetch(`/api/match/seeker/${seekerId}`).then(r => r.json());
}

// ... 모든 API에 동일 패턴
```

### 더미 데이터 파일

```
frontend/src/lib/dummy/
├── user.ts              → Alice(구직자), Bob(채용담당자) 프로필
├── resume.ts            → 완성된 이력서 + 시장가치
├── datasources.ts       → 연결된 데이터소스 목록
├── jobs.ts              → 채용공고 3개
├── matches.ts           → 매칭 결과 Top-5
├── negotiation.ts       → 협상 세션 + 5라운드 데이터
├── agreement.ts         → 합의 결과 + 온체인 기록
└── escrow.ts            → 에스크로 잔액 + 결제 내역
```

## 4. 페이지별 상세

### 4-1. 로그인 페이지 (`/`)

```
[NEAR 지갑으로 로그인] 버튼
  → near-connect로 지갑 연결
  → POST /auth/near/challenge → 서명 요청
  → POST /auth/near/verify → JWT 수신
  → role에 따라 대시보드로 이동
    - SEEKER → /dashboard/seeker
    - EMPLOYER → /dashboard/employer

더미: 지갑 연결 없이 Alice/Bob 선택 → 바로 대시보드
```

### 4-2. 구직자 대시보드 (`/dashboard/seeker`)

```
┌─────────────────────────────────────────┐
│  데이터소스 연결 상태                      │
│  [GitHub ✓] [Slack ✓] [Discord ✓] [정부24 ✓] │
│  [+ 데이터소스 추가]                       │
├─────────────────────────────────────────┤
│  내 이력서                                │
│  상태: 완성 ✓                             │
│  기술: TypeScript, React, NestJS, ...    │
│  경력: 3년 (프론트엔드/백엔드)              │
│  [이력서 상세 보기]  [이력서 재생성]          │
├─────────────────────────────────────────┤
│  시장가치                                 │
│  적정 연봉: 6,000만 ~ 7,500만원            │
│  강점: TypeScript 풀스택, 오픈소스 기여      │
│  [상세 분석 보기]                           │
├─────────────────────────────────────────┤
│  매칭 공고 (Top-5)                        │
│  1. 백엔드 개발자 - Company A  [동의] [거절] │
│  2. 풀스택 개발자 - Company B  [동의] [거절] │
│  ...                                     │
├─────────────────────────────────────────┤
│  진행 중인 협상                             │
│  Company A - Round 3/7 진행 중  [모니터링]   │
│  Company B - 합의 완료 ✓       [결과 보기]   │
└─────────────────────────────────────────┘
```

### 4-3. 채용담당자 대시보드 (`/dashboard/employer`)

```
┌─────────────────────────────────────────┐
│  에스크로 잔액: 5.0 NEAR                   │
│  [추가 예치]                               │
├─────────────────────────────────────────┤
│  내 채용공고                               │
│  1. 백엔드 개발자 (ACTIVE)  [수정] [마감]    │
│  [+ 새 공고 작성]                           │
├─────────────────────────────────────────┤
│  매칭 후보자 (Top-5)                       │
│  1. Alice - 매칭 87%  [동의] [거절]         │
│  2. Charlie - 매칭 82%  [동의] [거절]       │
│  ...                                     │
├─────────────────────────────────────────┤
│  진행 중인 협상                             │
│  Alice - Round 3/7 진행 중  [모니터링]       │
└─────────────────────────────────────────┘
```

### 4-4. 데이터소스 연결 페이지 (`/datasource`)

```
각 소스별 카드:
  [GitHub]  → "연결하기" 클릭 → OAuth 리다이렉트 (실제)
  [Slack]   → "연결하기" 클릭 → mock 연결 → "연결됨" 표시
  [Discord] → "연결하기" 클릭 → mock 연결 → "연결됨" 표시
  [정부24]  → "연결하기" 클릭 → mock 연결 → "연결됨" 표시

연결된 소스: 마지막 동기화 시간 + [동기화] 버튼
```

### 4-5. 이력서 상세 (`/resume`)

```
[이력서 생성] 버튼 클릭 시:
  → 상태 표시: "수집 중..." → "분석 중..." → "완성!"
  → 프로그레스 바 또는 스텝 인디케이터

완성 후:
  - 기술 태그 목록
  - 경력 타임라인
  - 학력 정보
  - AI 분석 요약
  - 시장가치 카드 (적정 연봉 범위 + 근거)
```

### 4-6. 대화형 공고 작성 (`/jobs/create`)

```
채팅 UI:
  에이전트: "어떤 포지션을 채용하시나요?"
  사용자: "백엔드 개발자요"
  에이전트: "필수 기술스택은 무엇인가요?"
  사용자: "Node.js, TypeScript"
  ...

또는 [폼으로 전환] 탭 → 직접 입력 폼

공고 완성 후 → 바운더리 설정 대화로 자동 전환:
  에이전트: "연봉 상한은 얼마까지 가능한가요?"
  ...
```

### 4-7. 에스크로 예치 (`/escrow`)

```
[예치 금액 입력] (NEAR 단위)
[예치하기] → 지갑 팝업 ① (deposit)
[에이전트 권한 부여] → 지갑 팝업 ② (addKey)

잔액 표시 + 결제 내역 목록
```

### 4-8. 협상 모니터링 (`/negotiation/:sessionId`) — 핵심 데모 화면

```
┌─────────────────────────────────────────┐
│  협상: Alice ↔ Company A                 │
│  상태: 진행 중 (Round 3/7)                │
├─────────────────────────────────────────┤
│  Round 1 - 채용 에이전트                   │
│  ┌─────────────────────────────────────┐│
│  │ 연봉: 6,500만                        ││
│  │ 근무: 주4일 출근                      ││
│  │ 직급: Senior Backend Engineer         ││
│  │ 근거: "공고 기준 초기 오퍼..."          ││
│  └─────────────────────────────────────┘│
│                                          │
│  Round 2 - 커리어 에이전트                  │
│  ┌─────────────────────────────────────┐│
│  │ 연봉: 7,000만 (↑500)                 ││
│  │ 근무: 주3일 출근 (↑1일)               ││
│  │ 시작일: 7/1                           ││
│  │ 근거: "시장가치 분석 기반..."           ││
│  └─────────────────────────────────────┘│
│                                          │
│  Round 3 - 진행 중... ⏳                  │
│                                          │
├─────────────────────────────────────────┤
│  [에이전트에게 지시 보내기]                  │
│  ┌─────────────────────────────────────┐│
│  │ "연봉은 6,800 이하로 양보하지 마"      ││
│  │                          [전송]      ││
│  └─────────────────────────────────────┘│
└─────────────────────────────────────────┘
```

### 4-9. 합의 확인 + 승인 (`/negotiation/:sessionId/agree`)

```
┌─────────────────────────────────────────┐
│  협상 합의 완료!                           │
├─────────────────────────────────────────┤
│  합의 내용:                                │
│  - 연봉: 6,800만원                        │
│  - 근무: 주3일 출근, 2일 재택               │
│  - 직급: Senior Backend Engineer           │
│  - 시작일: 2026-07-01                      │
│  - 수습기간: 3개월                          │
├─────────────────────────────────────────┤
│  [승인] [거절]                             │
│                                           │
│  ⚠ 승인 시 온체인에 기록되고                 │
│    에스크로 결제가 자동 실행됩니다              │
└─────────────────────────────────────────┘

양측 모두 승인 후:
  → "온체인 기록 완료! TX: abc123..."
  → "에스크로 결제 완료! TX: def456..."
  → "면접 프로세스로 진행합니다"
```

### 4-10. 협상 히스토리 (`/negotiation/:sessionId/history`)

```
ECDH 복호화 (클라이언트 사이드):
  1. near-connect에서 사용자 개인키 접근
  2. tweetnacl로 Ed25519 → Curve25519 변환
  3. 상대방 공개키 + 내 개인키 → ECDH → session_key
  4. 각 라운드 encrypted_data 복호화
  5. 복호화된 전체 라운드 표시

승연과 협의 필요: 복호화 로직 공유
```

## 5. 더미 → 실제 전환 일정

| Day | 작업 |
|-----|------|
| Day 1 오전 (4/12) | 프로젝트 셋업 + 더미 데이터 전체 작성 + 로그인 UI |
| Day 1 오후 (4/12) | 대시보드(구직자/채용) + 데이터소스 연결 UI |
| Day 2 오전 (4/13) | 이력서/공고/에스크로 페이지 (더미) |
| Day 2 오후 (4/13) | 협상 모니터링 + 합의 확인 + 히스토리 페이지 (더미) |
| ✅ Day 2 마감 | 더미 데이터로 전체 플로우 시연 가능 |
| Day 3 (4/14) | 더미 → 실제 API 전환 (Auth → 이력서 → 공고 → 매칭) |
| Day 4 (4/15) | 협상 API 연결 + ECDH 복호화 구현 (승연과 페어) + 에스크로 연결 |
| Day 5 (4/16) | UX 폴리싱 + 에러 핸들링 + 데모 리허설 |
| Day 6 (4/17) | 최종 리허설 + 제출 |

## 6. 프론트엔드 ↔ 백엔드 API 매핑

| 페이지 | 사용 API | 담당 백엔드 |
|--------|---------|-----------|
| 로그인 | POST /auth/near/* | 성훈 |
| 데이터소스 | GET/POST /datasource/* | 준하 |
| 이력서 | GET/POST /resume/* | 준하 |
| 공고 작성 | POST /jobs/chat, /jobs | 승연 |
| 에스크로 | POST /escrow/*, GET /escrow/* | 성훈 |
| 매칭 | GET /match/* | 준하 |
| 협상 | GET/POST /negotiation/* | 승연 |
| 합의 조회 | GET /agreement/* | 승연 |

## 7. 수락 기준

- [ ] NEAR 지갑 로그인 → JWT 수신 → role별 대시보드 이동
- [ ] 더미 데이터로 전체 플로우 시연 가능 (로그인→이력서→공고→매칭→협상→합의)
- [ ] 데이터소스 연결 UI → GitHub 실제 OAuth + 나머지 mock
- [ ] 이력서 생성 상태 표시 (수집 중 → 분석 중 → 완성)
- [ ] 대화형 공고 작성 챗 UI 동작
- [ ] 협상 모니터링 화면 → 라운드별 실시간 표시
- [ ] 사용자 개입 입력 → 에이전트에 전달
- [ ] 합의 확인 + 승인 → 온체인 기록 + 결제 결과 표시
- [ ] ECDH 클라이언트 복호화 → 협상 히스토리 전체 열람
